export type STRING = string;
export type NUMBER = number;
export type BOOLEAN = boolean;
export type USER = {
    id: number,
    name: string,
    lastName?: string,
    email: string,
    password: number,
    age?: number
}
